---
schema: nenjo.package.v1
name: nenji
version: "1.0.2"
description: Nenjo platform guide, designer, reviewer, and orchestrator.

dependencies:
  commands: "^1.0.0"
  context: "^1.0.0"
  knowledge: "^1.0.0"

modules:
  - context/
  - agent.yaml
