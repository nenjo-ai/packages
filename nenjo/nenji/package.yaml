---
schema: nenjo.package.v1
name: nenji
version: "1.6.0"
description: Nenjo platform guide, designer, reviewer, and orchestrator.

dependencies:
  capabilities: "^1.5.0"
  commands: "^1.5.0"
  context: "^1.5.0"
  knowledge: "^1.7.0"

modules:
  - context/
  - agent.yaml
