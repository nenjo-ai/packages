---
schema: nenjo.package.v1
name: nenji
version: "1.1.0"
description: Nenjo platform guide, designer, reviewer, and orchestrator.

dependencies:
  commands: "^1.1.0"
  context: "^1.0.3"
  knowledge: "^1.0.2"

modules:
  - context/
  - agent.yaml
