---
schema: nenjo.package.v1
name: nenji
version: "1.5.0"
description: Nenjo platform guide, designer, reviewer, and orchestrator.

dependencies:
  capabilities: "^1.3.0"
  commands: "^1.4.0"
  context: "^1.4.0"
  knowledge: "^1.6.0"

modules:
  - context/
  - agent.yaml
